# ── Runtime mode ──────────────────────────────────────────────────────────────
# Set to "true" to run as a RunPod serverless handler instead of HTTP server.
RUNPOD_ENABLED=false

# Pre-load all model weights at startup instead of on the first request.
# Requires a persistent volume at HF_HOME to avoid re-downloading every restart.
PRELOAD_MODELS=false

# Flask / Gunicorn (ignored when RUNPOD_ENABLED=true)
PORT=5000

# HuggingFace — só obrigatório se o modelo for gated (FLUX.1-dev, por exemplo)
# HF_TOKEN=hf_...

# Modelo a carregar (padrão: black-forest-labs/FLUX.1-schnell)
# MODEL_ID=black-forest-labs/FLUX.1-schnell

# Diretório de cache do HuggingFace
# HF_HOME=/cache/huggingface

# ControlNet model for image-guided generation (open, Apache 2.0)
# CONTROLNET_MODEL_ID=InstantX/FLUX.1-dev-Controlnet-Canny
